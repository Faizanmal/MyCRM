from django.apps import AppConfig

class MetaverseExperiencesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'metaverse_experiences'
    verbose_name = 'Metaverse Virtual Experiences'
